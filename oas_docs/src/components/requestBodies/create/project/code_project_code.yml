---
components:
  requestBodies:
    create_project_codeProjectCode:
      content:
        application/json:
          schema:
            properties:
              project_code:
                "$ref": "#/components/schemas/ProjectCodeInput"
      description: プロジェクトのパラメータ
      required: true
