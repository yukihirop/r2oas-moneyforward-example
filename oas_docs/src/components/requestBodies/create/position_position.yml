---
components:
  requestBodies:
    create_positionPosition:
      content:
        application/json:
          schema:
            properties:
              position:
                "$ref": "#/components/schemas/PositionInput"
      description: 役職のパラメータ
      required: true
